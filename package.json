{
  "name": "mundial-quiniela",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node --no-warnings server.js"
  }
}
